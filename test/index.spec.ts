import { WalletPaySDK } from '../src';

describe('index', () => {
  describe('WalletPaySDK', () => {
    it('Init test', () => {
      const result = new WalletPaySDK({ apiKey: 'TEST_API_KEY' });

      // expect(result).toMatch();
    });
  });
});
